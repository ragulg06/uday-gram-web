You are an expert full-stack TypeScript + Node.js + React engineer and ML engineer. You are working inside a cloned repository of the AGY / Uday Gram portal (React client + Node server + shared Drizzle schema). Your job is to (1) convert the prototype into a real server-side app (Postgres + Drizzle, RBAC, uploads, validation, email) and (2) implement the SIH solution features (ML components tightly integrated into the workflow) so the project is demo-ready and competitive.

Reference the official PMAGY guidelines and the AGY user manual when implementing validations, flows, and formats. Use these documents as authoritative sources in the code/logic. 

PMAGY_Guidelines-Revised_July_2…

 

User Manual of AGY

HIGH-LEVEL OBJECTIVES (deliverables)

Replace in-memory storage with Postgres + Drizzle using shared/schema.ts. Implement PostgresStorage that fully implements IStorage.

Implement secure auth (JWT) and RBAC roles: admin, state, district, block, village. Protect all endpoints.

Expose complete REST API powering the client (villages, surveys, VDPs, works, uploads, progress, scores, MPR). Update client to call APIs.

Add file upload endpoints & storage (uploads table + store files to uploads/ or S3 if env provided).

Add server-side validations following PMAGY / AGY formats (Formats I–VI, VDP lock rules, score thresholds, required members, mandatory fields). Use Zod + extra rules. Cite manuals where rules apply. 

PMAGY_Guidelines-Revised_July_2…

 

User Manual of AGY

Add email notifications (Nodemailer) for VDP submit/finalize/unlock-request/milestones.

Add audit logs, health-check, sensible error handling, and .env config.

Add ML modules and integrate them into the backend:

Prioritizer service: XGBoost/LightGBM (production: RF/XGBoost) that predicts impact_points and exposes POST /api/prioritize to return ranked VDP items + budget allocation (ILP + greedy fallback).

Photo verifier: small vision model (MobileNet/EfficientNet-lite) for binary verification of "work complete" with on-device TFLite export and backend inference fallback. Endpoint: POST /api/verify-photo.

Satellite change detector: baseline NDVI/NDBI bi-temporal script + optional U-Net model for small-area change detection. Endpoint: POST /api/satellite-check.

OCR + NER extractor for sanction orders and convergence docs (Tesseract + lightweight NER or regex) to auto-fill convergence sources in VDP.

Anomaly detector for survey data (IsolationForest) to flag suspicious inputs.

Explainability: return SHAP summaries for prioritzier predictions.

Provide demo scripts: DB migration, seed data (villages + sample VDPs + synthetic works + photos), Postgres Docker Compose, and a run-demo.sh that populates data and runs the server and ML services.

Write acceptance tests (API smoke tests) and a short demo script (what the judge clicks) that shows survey → VDP → prioritizer → allocate funds → upload progress → photo verification → indicator updated → MPR exported.

CONCRETE TASKS & IMPLEMENTATION DETAILS
A. Database & Backend wiring

Create server/db.ts connecting to Postgres via DATABASE_URL. Use Drizzle client and import all pgTables from @shared/schema.

Add migrations (drizzle or SQL) to create tables. Add npm run db:migrate and npm run db:seed.

Implement server/postgresStorage.ts implementing IStorage:

Methods at minimum: createUser, getUserById, getUserByUsername, listVillages, createVillage, getVillage, createHouseholdSurvey, createInfrastructureSurvey, computeIndicatorScores, createVDP, updateVDPStatus, addWork, addWorkProgress, listWorks, saveUpload, getUploads.

Replace MemStorage usage with PostgresStorage instantiation in server start.

B. API endpoints (examples — implement all needed)

Auth: POST /api/auth/login, POST /api/auth/register (admin only), GET /api/auth/me.

Villages: GET /api/villages, GET /api/villages/:id, POST /api/villages (admin/district), GET /api/villages/:id/score.

Surveys: POST /api/villages/:id/surveys/household (Format III-A), POST /api/villages/:id/surveys/infrastructure (Format II).

VDPs: POST /api/vdp, GET /api/vdp/:id, PUT /api/vdp/:id, POST /api/vdp/:id/finalize, POST /api/vdp/:id/unlock-request.

Works: POST /api/vdp/:id/works, POST /api/works/:id/progress (geotagged photos + percent).

Uploads: POST /api/uploads/vdp-document, POST /api/uploads/progress-photo.

ML: POST /api/prioritize (body: vdpItems[], budget), POST /api/verify-photo (file + metadata), POST /api/satellite-check (village id + dates), POST /api/ocr-extract.

Notifications: server triggers emails on finalize, unlock-request, milestone thresholds.

C. RBAC & Auth

Use JWT with refresh tokens or session cookies; implement authMiddleware that attaches req.user with role and scope (state/district/block/village).

Implement requireRole(roleOrArray) middleware. Enforce per-route role constraints (District can create/finalize VDPs for villages in their district; State approves unlock and declarations).

D. File uploads

Use Multer; sanitize filenames and store metadata in an uploads table.

Support local storage default; optional S3 if env configured.

Expose signed URLs when using S3; for local show /uploads/:filename.

E. Server-side validation (Zod + custom)

Use generated Zod schemas from shared/schema.ts for shape, plus custom checks from PMAGY & AGY manual:

Village Format I: require GPS, VLCC members list (≥1 SC member? manual rules).

Format II/III: all domains answered; action plan lines if gap exists.

VDP finalize: all required formats completed, score computed, funds plan assigned.

Declare Adarsh Gram: ensure score threshold (configurable; default 100 or award threshold rule per manual), ODF requirement, photos uploaded. 

PMAGY_Guidelines-Revised_July_2…

 

User Manual of AGY

Return human-readable error JSON for validation failures.

F. ML modules — design & integration

Prioritizer

Training: XGBoost or RandomForest on tabular VDP/work features (affected_hhs, baseline_gap, cost, convergence_probability, urgency, distance). Provide training notebook and save model artifact.

Serving: backend loads model (ONNX or joblib) and returns predicted impact and SHAP explanation for each vdp item. ILP (OR-Tools) + greedy fallback used to allocate under budget.

Endpoint: POST /api/prioritize returns ranked items, chosen set, expected indicator uplift, and SHAP per item.

Photo verifier

Small classifier (MobileNetV2) trained on labeled before/during/after images. Provide training script and TFLite conversion.

Serve two ways:

On-device TFLite inference (for mobile demo), and/or

Backend TensorFlow/PyTorch microservice for heavier inference.

Endpoint: POST /api/verify-photo returns {verified: bool, score: float, explain_vis: url_or_base64}.

Satellite change detector

Baseline: implement NDVI/NDBI difference pipeline using Sentinel-2 tiles for village polygon (download script). Provide simple bi-temporal differencing result and thresholding for demo.

Optional CNN-based change detection: provide U-Net training stub.

Endpoint: POST /api/satellite-check returns geometry of detected change and confidence.

OCR & NER

Tesseract OCR pipeline to extract text from uploads.

Regex/NER to extract amounts, scheme names, sanction dates; auto-fill VDP convergence sources.

Endpoint: POST /api/ocr-extract.

Anomaly detection

Use IsolationForest to flag survey entries with improbable values (GPS mismatch, identical photos, duplicate HHIDs).

Integrate flagging into survey ingest pipeline and show needs_verification: true in response.

Explainability

Use SHAP for tabular model outputs and return short human-readable reasons (e.g., "High impact because closes 2 indicator points for 120 HH, high convergence probability").

G. Offline / Mobile considerations

Keep server lightweight for offline-first: mobile app will store surveys locally and sync. Provide a sync endpoint POST /api/sync to push queued records.

Provide TFLite model for photo verification to run on-device when offline and fallback to backend when online.

H. Deployment + demo infra

Provide docker-compose.yml with services: postgres, pgadmin (optional), backend, ml-service (optional), frontend (dev or static), and minio (optional).

run-demo.sh:

Start containers

Run migrations

Seed demo data (10 villages, sample VDPs, sample works, sample images)

Start worker (for emails & ML)

Include a README section "How judges demo" with click-by-click steps for the live presentation.

ACCEPTANCE CRITERIA (how I will verify)

Postgres DB exists and npm run db:migrate creates required tables from shared/schema.ts.

server uses PostgresStorage — data persists across restarts.

Client shows real data (no fixtures) and role views differ by logged-in role.

Uploads saved and retrievable via UI.

VDP finalize triggers email to State and locks edits; unlock-request flows to State and can be approved.

POST /api/prioritize returns ranked items + selected items under sample budgets; SHAP explanations included.

POST /api/verify-photo returns verification and score for demo images.

Demo script runs end-to-end and a judge can perform the survey→VDP→prioritize→execute→upload→verify flow.

EXTRA NOTES (prioritize for SIH)

Focus on reliable prioritizer + photo verification + offline collection — judges care about automation that removes manual burden. The rest (OCR, satellite U-Net) are extras if time allows.

Keep ML explainable (SHAP + visual Grad-CAM/heatmaps) — crucial for government adoption.

Keep the UI simple and show clear evidence (photos + GPS + model score) for each completed work.

Cite PMAGY monitoring and formats in validation rules. 

PMAGY_Guidelines-Revised_July_2…

FINAL INSTRUCTION TO THE AGENT

Do the work incrementally and push changes to branches with clear commits:

feature/db-postgres

feature/auth-rbac

feature/uploads

feature/validation

feature/prioritizer

feature/photo-verifier

feature/demo-deploy

Include unit tests / smoke tests and a top-level README describing how to run locally and how to demo for judges.